[tool.poetry]
name = "loom"
version = "0.1.0"
description = "A low-code platform for developers. You design integration flows visually; Loom generates a real, deployable Python service. The generated project is yours — check it into Git, debug it normally, deploy it like any other Flask app.."
authors = ["Arun Raghunath <noreply@example.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"

[tool.poetry.group.dev.dependencies]
pytest = "^9.0.3"
ruff = "^0.15"
mypy = "^1.20.2"
pre-commit = "^3.8"

[tool.ruff]
line-length = 88
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF", "N", "PT"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "src"
packages = ["src", "tests"]
explicit_package_bases = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra --strict-markers --strict-config"

[build-system]
requires = ["poetry-core>=1.9.0"]
build-backend = "poetry.core.masonry.api"
